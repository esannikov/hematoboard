# HematoBoard v1 public archive

This directory preserves the selector and release-authenticity receipt used by the first-generation public dashboard. The legacy case payloads remain at their historical root paths so old links continue to resolve.

The current dashboard uses only V2-clean public aliases from the root `case-index.json`. Public aliases never rename the immutable internal case or reasoning artifacts.
